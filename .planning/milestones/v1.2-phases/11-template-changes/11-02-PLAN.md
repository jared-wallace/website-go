---
phase: 11-template-changes
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - web/templates/base.html
  - web/templates/list.html
  - web/static/main.css
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04
  - ATMO-03
  - TYPO-03

must_haves:
  truths:
    - "About link does not appear in the primary navigation bar"
    - "Footer displays a two-section layout with nav links left and copyright right"
    - "Footer contains the phrase 'Still anchored. Still writing.'"
    - "Both nav elements have distinct aria-labels for screen reader disambiguation"
    - "Rope divider renders as an inline SVG, not a dashed HR"
    - "List page shows 'The Wild Meridian' h1 heading and tagline above posts"
  artifacts:
    - path: "web/templates/base.html"
      provides: "Restructured nav and footer with SVG rope"
      contains: "aria-label=\"Main navigation\""
    - path: "web/templates/list.html"
      provides: "Hero heading block"
      contains: "list-hero-title"
    - path: "web/static/main.css"
      provides: "Phase 11 CSS block with footer layout, hero, and rope styles"
      contains: "Phase 11: Template Changes"
  key_links:
    - from: "web/templates/base.html"
      to: "web/static/main.css"
      via: "CSS classes referenced in HTML"
      pattern: "footer-inner|footer-copyright|footer-phrase|rope-divider"
    - from: "web/templates/list.html"
      to: "web/static/main.css"
      via: "Hero CSS classes"
      pattern: "list-hero|list-hero-title|list-hero-tagline"
---

<objective>
Implement all Phase 11 template and CSS changes: restructure the nav bar (remove About link, add aria-label), rebuild the footer (two-section layout, personality phrase, footer nav with About + RSS, aria-label), replace the dashed HR rope divider with an inline SVG twisted rope, add the hero heading to the list page, and append the Phase 11 CSS block to main.css.

Purpose: Complete the v1.2 Shore Leave Polish milestone with the final visual and structural improvements.
Output: Updated base.html, list.html, and main.css with all six requirements satisfied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-template-changes/11-CONTEXT.md
@.planning/phases/11-template-changes/11-RESEARCH.md
@.planning/phases/11-template-changes/11-UI-SPEC.md
@.planning/phases/11-template-changes/11-01-SUMMARY.md
@web/templates/base.html
@web/templates/list.html
@web/static/main.css
</context>

<interfaces>
<!-- Key patterns from the existing codebase that the executor needs -->

From web/templates/base.html (current nav, lines 26-46):
```html
<nav class="site-nav">
  <a href="/" class="site-name">The Wild Meridian</a>
  <span class="site-tagline">dispatches from the deep end</span>
  <a href="/about" class="nav-link">About</a>
  <button class="dark-toggle" id="dark-toggle" aria-label="Switch to dark mode">
    <!-- sun/moon SVGs -->
  </button>
</nav>
```

From web/templates/base.html (current footer, lines 50-64):
```html
<footer class="site-footer">
  <hr class="rope-divider">
  <p>
    &copy; {{.Year}} Jared Wallace
    <a href="/rss" class="rss-link" aria-label="RSS feed"><!-- RSS SVG --></a>
  </p>
</footer>
```

From web/static/main.css (rope divider rule, lines 443-450):
```css
.rope-divider {
  border: none;
  border-top: 2px dashed var(--color-divider);
  opacity: 0.6;
  margin: 24px 0;
}
```

From web/static/main.css (footer rules, lines 576-610):
```css
.site-footer { text-align: center; padding: 24px; font-size: 14px; color: var(--color-text-muted); }
.site-footer .rope-divider { margin-bottom: 16px; }
.site-footer p { margin: 0; display: flex; align-items: center; justify-content: center; }
.rss-link { display: inline-flex; align-items: center; ... }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Restructure base.html nav + footer and replace rope divider with SVG</name>
  <files>web/templates/base.html</files>
  <read_first>
    - web/templates/base.html (full current markup — nav lines 26-46, footer lines 50-64)
    - .planning/phases/11-template-changes/11-CONTEXT.md (locked decisions D-01 through D-10)
    - .planning/phases/11-template-changes/11-UI-SPEC.md (copywriting contract, interaction contract)
    - .planning/phases/11-template-changes/11-RESEARCH.md (SVG rope pattern, footer HTML structure, anti-patterns)
  </read_first>
  <action>
Edit `web/templates/base.html` with three changes:

**Change 1 — Nav bar (line 26-46):** Per D-09 and D-10, update the `<nav>` opening tag and remove the About link.

Replace:
```html
<nav class="site-nav">
  <a href="/" class="site-name">The Wild Meridian</a>
  <span class="site-tagline">dispatches from the deep end</span>
  <a href="/about" class="nav-link">About</a>
  <button class="dark-toggle" ...>
```

With:
```html
<nav class="site-nav" aria-label="Main navigation">
  <a href="/" class="site-name">The Wild Meridian</a>
  <span class="site-tagline">dispatches from the deep end</span>
  <button class="dark-toggle" ...>
```

The ONLY changes are: (1) add `aria-label="Main navigation"` to the nav tag, (2) delete the entire `<a href="/about" class="nav-link">About</a>` line. The dark toggle button and its SVG contents are UNCHANGED.

**Change 2 — Footer (lines 50-64):** Per D-01, D-02, D-03, D-10, replace the entire footer contents.

Replace lines 50-64 with:
```html
  <footer class="site-footer">
    <svg class="rope-divider" xmlns="http://www.w3.org/2000/svg"
         width="100%" height="12" aria-hidden="true" preserveAspectRatio="none"
         viewBox="0 0 300 12">
      <path d="M0,6 C10,1 20,11 30,6 C40,1 50,11 60,6 C70,1 80,11 90,6 C100,1 110,11 120,6 C130,1 140,11 150,6 C160,1 170,11 180,6 C190,1 200,11 210,6 C220,1 230,11 240,6 C250,1 260,11 270,6 C280,1 290,11 300,6"
            fill="none" stroke="var(--color-divider)" stroke-width="2" stroke-linecap="round"/>
      <path d="M0,6 C10,11 20,1 30,6 C40,11 50,1 60,6 C70,11 80,1 90,6 C100,11 110,1 120,6 C130,11 140,1 150,6 C160,11 170,1 180,6 C190,11 200,1 210,6 C220,11 230,1 240,6 C250,11 260,1 270,6 C280,11 290,1 300,6"
            fill="none" stroke="var(--color-divider)" stroke-width="2" stroke-linecap="round" opacity="0.7"/>
    </svg>
    <div class="footer-inner">
      <nav aria-label="Footer navigation">
        <a href="/about" class="nav-link">About</a>
        <a href="/rss" class="rss-link" aria-label="RSS feed">
          <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24"
               fill="none" stroke="currentColor" stroke-width="2"
               stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M4 11a9 9 0 0 1 9 9"></path>
            <path d="M4 4a16 16 0 0 1 16 16"></path>
            <circle cx="5" cy="19" r="1" fill="currentColor" stroke="none"></circle>
          </svg>
        </a>
      </nav>
      <div class="footer-copyright">
        <p class="footer-phrase">Still anchored. Still writing.</p>
        <p class="footer-copyright-line">&copy; {{.Year}} Jared Wallace</p>
      </div>
    </div>
  </footer>
```

Key details:
- The `<hr class="rope-divider">` is replaced with the inline `<svg class="rope-divider">` (per D-04, D-05, D-06)
- SVG has `viewBox="0 0 300 12"` — CRITICAL: must include viewBox for preserveAspectRatio="none" to work
- Both SVG strands use `stroke="var(--color-divider)"` — NOT a hex value, NOT currentColor
- SVG has `aria-hidden="true"` — decorative element
- Footer nav contains About link + RSS icon ONLY (per D-02)
- RSS SVG icon is preserved exactly from the current footer
- Personality phrase "Still anchored. Still writing." in `.footer-phrase` (per D-03)
- Copyright uses `.footer-copyright-line` class to avoid `.site-footer p` flex collision

**DO NOT:**
- Change the dark toggle button SVGs
- Add inline style attributes
- Use `stroke="currentColor"` on the rope SVG (use `var(--color-divider)` per D-05)
- Add Home or Posts links to footer nav (per D-02)
  </action>
  <verify>
    <automated>cd /Users/jaredwallace/src/jared-wallace/website-go && go test ./internal/handler/blog/... -run "TestNav|TestFooter|TestRope" -v 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - base.html contains `aria-label="Main navigation"` on the site-nav element
    - base.html does NOT contain `<a href="/about" class="nav-link">About</a>` between `site-nav` and `</nav>` (About link removed from nav)
    - base.html contains `<svg class="rope-divider"` (SVG rope present)
    - base.html does NOT contain `<hr class="rope-divider">` (old HR removed)
    - base.html contains `class="footer-inner"` (two-section wrapper)
    - base.html contains `aria-label="Footer navigation"` (footer nav landmark)
    - base.html contains `class="footer-phrase"` with text "Still anchored. Still writing."
    - base.html contains `stroke="var(--color-divider)"` in SVG paths (not hex, not currentColor)
    - base.html contains `aria-hidden="true"` on the rope SVG
    - base.html contains `viewBox="0 0 300 12"` on the rope SVG
    - base.html contains `preserveAspectRatio="none"` on the rope SVG
    - TestNavAboutLinkRemoved, TestFooterTwoSection, TestFooterPersonalityPhrase, TestNavAriaLabels, TestRopeDividerSVG all pass
  </acceptance_criteria>
  <done>Nav restructured (About removed, aria-label added), footer rebuilt with two-section layout, personality phrase, footer nav with About + RSS, and inline SVG rope divider. Five of six Phase 11 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add list page hero and Phase 11 CSS block</name>
  <files>web/templates/list.html, web/static/main.css</files>
  <read_first>
    - web/templates/list.html (current template — hero inserts before line 19 {{if not .Posts}})
    - web/static/main.css (full file — need to update rope-divider rule at line 445, footer rules at lines 576-594, and append Phase 11 block at end)
    - .planning/phases/11-template-changes/11-UI-SPEC.md (typography and spacing specs for hero and footer)
    - .planning/phases/11-template-changes/11-RESEARCH.md (CSS code examples, .site-footer p collision fix)
  </read_first>
  <action>
Two files to edit:

**File 1 — web/templates/list.html:** Per D-07 and D-08, insert the hero block inside the container div, immediately after `<div class="container">` (line 18) and before the `{{if not .Posts}}` conditional (line 19).

Insert these 4 lines between `<div class="container">` and `{{if not .Posts}}`:
```html
  <div class="list-hero">
    <h1 class="list-hero-title">The Wild Meridian</h1>
    <p class="list-hero-tagline">dispatches from the deep end</p>
  </div>
```

The hero is UNCONDITIONAL — it renders even when there are no posts. It sits outside the `{{if not .Posts}}` / `{{else}}` conditional.

**File 2 — web/static/main.css:** Three edits:

**Edit A — Replace the rope-divider rule (line 443-450).** Replace:
```css
/* --- Rope Divider --- */

.rope-divider {
  border: none;
  border-top: 2px dashed var(--color-divider);
  opacity: 0.6;
  margin: 24px 0;
}
```

With:
```css
/* --- Rope Divider --- */

.rope-divider {
  display: block;
  width: 100%;
  height: 12px;
  margin: 24px 0;
  overflow: visible;
}
```

This removes the dashed border properties (no longer needed — the SVG carries the visual) and sets proper block layout for the inline SVG element.

**Edit B — Update footer rules (lines 576-594).** Replace:
```css
/* --- Footer --- */

.site-footer {
  text-align: center;
  padding: 24px;
  font-size: 14px;
  color: var(--color-text-muted);
}

.site-footer .rope-divider {
  margin-bottom: 16px;
}

.site-footer p {
  margin: 0;
  display: flex;
  align-items: center;
  justify-content: center;
}
```

With:
```css
/* --- Footer --- */

.site-footer {
  padding: 24px;
  font-size: 14px;
  color: var(--color-text-muted);
}
```

Removing: (1) `text-align: center` — the flex layout handles alignment now, (2) `.site-footer .rope-divider` override — base rule's `margin: 24px 0` is correct, (3) `.site-footer p` flex rule — this was for the old single-line layout and would interfere with the new multi-paragraph footer. The `.rss-link` rules that follow (lines 596-610) are UNCHANGED.

**Edit C — Append Phase 11 CSS block at end of main.css** (after the last existing rule). Add:
```css

/* === Phase 11: Template Changes === */

.footer-inner {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 24px;
}

.footer-copyright {
  text-align: right;
}

.footer-phrase {
  margin: 0 0 4px;
  font-style: italic;
  font-size: 14px;
  color: var(--color-text-muted);
}

.footer-copyright-line {
  margin: 0;
}

.list-hero {
  margin-bottom: 32px;
}

.list-hero-title {
  font-family: var(--font-display);
  font-size: 28px;
  font-weight: 700;
  line-height: 1.2;
  margin: 0 0 8px;
}

.list-hero-tagline {
  font-family: var(--font-body);
  font-style: italic;
  font-size: 18px;
  font-weight: 400;
  line-height: 1.5;
  color: var(--color-text-muted);
  margin: 0;
}

@media (max-width: 767px) {
  .footer-inner {
    flex-direction: column;
    align-items: center;
    gap: 16px;
  }

  .footer-copyright {
    text-align: center;
  }
}
```

**DO NOT:**
- Create a new CSS file (all changes in main.css per project rule)
- Use inline styles in templates
- Add `text-align: center` back to `.site-footer` (flex handles it)
- Forget the `.footer-copyright-line` class (prevents `.site-footer p` collision if any remnant exists)
  </action>
  <verify>
    <automated>cd /Users/jaredwallace/src/jared-wallace/website-go && go test ./internal/handler/blog/... -v 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - list.html contains `class="list-hero"` before the `{{if not .Posts}}` conditional
    - list.html contains `class="list-hero-title">The Wild Meridian</h1>`
    - list.html contains `class="list-hero-tagline">dispatches from the deep end</p>`
    - main.css contains `/* === Phase 11: Template Changes === */`
    - main.css contains `.footer-inner` with `display: flex`
    - main.css contains `.footer-copyright` with `text-align: right`
    - main.css contains `.footer-phrase` with `font-style: italic`
    - main.css contains `.list-hero` with `margin-bottom: 32px`
    - main.css contains `.list-hero-title` with `font-family: var(--font-display)` and `font-size: 28px`
    - main.css contains `.list-hero-tagline` with `font-style: italic` and `font-size: 18px`
    - main.css contains `@media (max-width: 767px)` with `.footer-inner` `flex-direction: column`
    - main.css `.rope-divider` rule contains `display: block` and does NOT contain `border-top: 2px dashed`
    - main.css does NOT contain `.site-footer .rope-divider` override rule
    - main.css does NOT contain `.site-footer p` with `display: flex`
    - `go test ./internal/handler/blog/... -v` — all tests pass including TestListHero
    - `go test ./...` — full suite green
  </acceptance_criteria>
  <done>List page hero heading renders above posts, Phase 11 CSS block provides footer layout, hero typography, and rope divider styles. All six Phase 11 tests pass. Full test suite green.</done>
</task>

</tasks>

<verification>
Full test suite passes: `go test ./...`
Phase 11 specific tests pass: `go test ./internal/handler/blog/... -run "TestNav|TestFooter|TestRope|TestListHero" -v`
No regressions in existing tests: TestListPostsEmpty, TestListOGMeta, TestPostOGMeta, TestShowPostNotFound, TestNotFound all still pass.
</verification>

<success_criteria>
- About link removed from nav bar, accessible from footer only
- Footer displays two-section layout (nav left, copyright right)
- Footer contains "Still anchored. Still writing." personality phrase
- Both nav elements have distinct aria-labels
- Rope divider is an inline SVG with twisted two-strand pattern using var(--color-divider)
- List page has "The Wild Meridian" h1 heading with "dispatches from the deep end" tagline
- All tests pass including six new Phase 11 tests
- No new CSS files created — all additions in main.css
</success_criteria>

<output>
After completion, create `.planning/phases/11-template-changes/11-02-SUMMARY.md`
</output>
