---
phase: 11-template-changes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/handler/blog/handler_test.go
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04
  - ATMO-03
  - TYPO-03

must_haves:
  truths:
    - "Six new test functions exist that will validate all Phase 11 template changes"
    - "All six tests fail (RED) because templates have not been updated yet"
  artifacts:
    - path: "internal/handler/blog/handler_test.go"
      provides: "Phase 11 validation test functions"
      contains: "TestNavAboutLinkRemoved"
  key_links:
    - from: "internal/handler/blog/handler_test.go"
      to: "web/templates/base.html"
      via: "handler renders templates, tests assert on HTML output"
      pattern: "strings\\.Contains"
---

<objective>
Write the six Wave 0 test functions that validate all Phase 11 template requirements. These tests use the existing `newTestHandler` + `strings.Contains` pattern already established in `handler_test.go`. All tests should fail (RED) against the current templates, confirming they will meaningfully validate the implementation in Plan 02.

Purpose: Establish automated verification before any template changes, following test-first discipline.
Output: Six new test functions in `handler_test.go`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-template-changes/11-CONTEXT.md
@.planning/phases/11-template-changes/11-RESEARCH.md
@.planning/phases/11-template-changes/11-VALIDATION.md
@internal/handler/blog/handler_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add six Phase 11 validation tests to handler_test.go</name>
  <files>internal/handler/blog/handler_test.go</files>
  <read_first>
    - internal/handler/blog/handler_test.go (current test patterns — use newTestHandler, httptest, strings.Contains)
    - web/templates/base.html (current nav/footer markup to understand what changes are expected)
    - web/templates/list.html (current list page to understand hero insertion point)
  </read_first>
  <action>
Append six new test functions to `internal/handler/blog/handler_test.go`, following the exact pattern used by `TestListPostsEmpty` and `TestListOGMeta`. Each test creates a handler via `newTestHandler`, issues a GET request, and asserts on the response body using `strings.Contains`.

Use this mock repository for all tests that need posts rendered:
```go
repo := &amp;mockRepository{
    posts: []model.Post{
        {Title: "Test Post", Slug: "test-post"},
    },
    totalCount: 1,
}
```

Use an empty mock `&amp;mockRepository{posts: nil, totalCount: 0}` for empty-state tests where needed (the hero should render even with no posts).

**Test 1: TestNavAboutLinkRemoved** (NAV-01)
- GET `/` via `h.ListPosts`
- Assert body does NOT contain `<a href="/about" class="nav-link">About</a>` inside the nav area
- Specifically: assert `strings.Contains(body, "site-nav")` is true (nav still exists), but the string `class="nav-link">About</a>` must NOT appear between `site-nav` and `</nav>`. Simplest approach: assert that `strings.Contains(body, "nav-link\">About")` is false (the About link with nav-link class should not exist in the nav).
- Note: The About link WILL exist in the footer. So check specifically that the body does NOT contain `<nav class="site-nav"` ... with an About link. The simplest reliable check: the string `class="nav-link">About</a>` should still appear (in footer), but `site-nav` section should not contain it. Use two assertions: (1) body contains `aria-label="Footer navigation"` (proves footer nav exists), (2) do NOT find the pattern `site-nav.*nav-link.*About` — but since strings.Contains is linear, the simplest approach is: split body on `</nav>` and check the first `<nav` block (site-nav) does not contain `About</a>`.

Actually, keep it simple and pragmatic — match the existing test style:
- Assert body does NOT contain the exact string: `<a href="/about" class="nav-link">About</a>\n    <button class="dark-toggle"` (the current nav markup has About immediately before the dark toggle button)
- Assert body DOES contain: `href="/about"` (About link still exists, just in footer now)

**Test 2: TestFooterTwoSection** (NAV-02)
- GET `/` via `h.ListPosts`
- Assert body contains `class="footer-inner"`
- Assert body contains `class="footer-copyright"`
- Assert body contains `aria-label="Footer navigation"`

**Test 3: TestFooterPersonalityPhrase** (NAV-03)
- GET `/` via `h.ListPosts`
- Assert body contains `class="footer-phrase"`
- Assert body contains `Still anchored. Still writing.`

**Test 4: TestNavAriaLabels** (NAV-04)
- GET `/` via `h.ListPosts`
- Assert body contains `aria-label="Main navigation"`
- Assert body contains `aria-label="Footer navigation"`

**Test 5: TestRopeDividerSVG** (ATMO-03)
- GET `/` via `h.ListPosts`
- Assert body contains `<svg class="rope-divider"` (new SVG element)
- Assert body does NOT contain `<hr class="rope-divider">` (old HR element gone)

**Test 6: TestListHero** (TYPO-03)
- GET `/` via `h.ListPosts` with EMPTY posts mock (hero renders even without posts)
- Assert body contains `class="list-hero"`
- Assert body contains `class="list-hero-title"`
- Assert body contains `The Wild Meridian</h1>` (h1 tag specifically)
- Assert body contains `dispatches from the deep end</p>` (tagline)
  </action>
  <verify>
    <automated>cd /Users/jaredwallace/src/jared-wallace/website-go && go build ./... 2>&amp;1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - handler_test.go contains `func TestNavAboutLinkRemoved(t *testing.T)`
    - handler_test.go contains `func TestFooterTwoSection(t *testing.T)`
    - handler_test.go contains `func TestFooterPersonalityPhrase(t *testing.T)`
    - handler_test.go contains `func TestNavAriaLabels(t *testing.T)`
    - handler_test.go contains `func TestRopeDividerSVG(t *testing.T)`
    - handler_test.go contains `func TestListHero(t *testing.T)`
    - `go build ./...` compiles without errors
    - Tests use `newTestHandler` and `strings.Contains` pattern matching existing tests
  </acceptance_criteria>
  <done>Six new test functions compile successfully and are ready to validate Plan 02 implementation. Tests are expected to fail (RED) against current unmodified templates.</done>
</task>

</tasks>

<verification>
All six test functions compile: `go build ./...` exits 0.
Tests exist: `go test ./internal/handler/blog/... -list "Test(Nav|Footer|Rope|List)" 2>&1` lists all six.
</verification>

<success_criteria>
- Six named test functions exist in handler_test.go
- Code compiles without errors
- Tests follow the established newTestHandler + strings.Contains pattern
</success_criteria>

<output>
After completion, create `.planning/phases/11-template-changes/11-01-SUMMARY.md`
</output>
