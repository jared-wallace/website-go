---
phase: 04-distribution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/templates/base.html
  - web/templates/post.html
  - web/templates/list.html
  - web/static/og-fallback.png
  - internal/service/post/get.go
  - internal/handler/blog/post.go
  - internal/handler/blog/handler_test.go
autonomous: true
requirements: [BLOG-06]

must_haves:
  truths:
    - "Sharing a post URL renders og:title matching the post title and og:description matching the post excerpt"
    - "Homepage HTML contains site-level OG tags with og:title 'The Log'"
    - "Every page advertises the RSS feed via link rel=alternate in the head"
    - "OG image tag points to /static/og-fallback.png on all pages"
    - "Twitter summary card meta tags are present on all pages"
  artifacts:
    - path: "web/templates/base.html"
      provides: "RSS auto-discovery link + default OG meta block in head"
      contains: "block \"meta\""
    - path: "web/templates/post.html"
      provides: "Per-post OG meta override with dynamic title/description/URL"
      contains: "define \"meta\""
    - path: "web/templates/list.html"
      provides: "Site-level OG meta override for listing page"
      contains: "define \"meta\""
    - path: "web/static/og-fallback.png"
      provides: "400x400 branded placeholder OG image"
    - path: "internal/service/post/get.go"
      provides: "Excerpt field on PostDetail struct"
      contains: "Excerpt"
    - path: "internal/handler/blog/handler_test.go"
      provides: "OG meta assertions for ShowPost and ListPosts responses"
      contains: "TestPostOGMeta"
  key_links:
    - from: "web/templates/post.html"
      to: "internal/handler/blog/post.go"
      via: "template data .Excerpt passed from handler"
      pattern: "Excerpt"
    - from: "web/templates/base.html"
      to: "web/templates/post.html"
      via: "block/define meta override mechanism"
      pattern: "block.*meta"
---

<objective>
Add Open Graph and Twitter Card meta tags to all public pages, RSS auto-discovery link to base template, and a fallback OG image.

Purpose: When readers share post URLs on Slack, Twitter, or other platforms, the link preview shows the correct title, description, and image (BLOG-06). RSS auto-discovery (D-04) lets feed readers find /rss automatically.
Output: Modified templates with meta blocks, Excerpt field added to PostDetail, OG fallback image, updated ShowPost handler, and OG meta test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-distribution/04-CONTEXT.md
@.planning/phases/04-distribution/04-RESEARCH.md
@.planning/phases/04-distribution/04-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From internal/service/post/get.go:
```go
type PostDetail struct {
	Post         model.Post
	RenderedHTML template.HTML
	ToC          []ToCEntry
	Tags         []string
	ReadingTime  int
}

func (s *Service) GetBySlug(ctx context.Context, slug string) (*PostDetail, error)
```

From internal/handler/blog/post.go:
```go
func (h *BlogHandler) ShowPost(w http.ResponseWriter, r *http.Request) {
	// ...
	h.render(w, http.StatusOK, "post.html", map[string]interface{}{
		"Post":         detail.Post,
		"RenderedHTML": detail.RenderedHTML,
		"ToC":          detail.ToC,
		"Tags":         detail.Tags,
		"ReadingTime":  detail.ReadingTime,
	})
}
```

From internal/service/post/excerpt.go:
```go
func Excerpt(markdownBody string, maxLen int) string
```

From internal/handler/blog/list.go:
```go
func (h *BlogHandler) ListPosts(w http.ResponseWriter, r *http.Request) {
	// ...
	h.render(w, http.StatusOK, "list.html", map[string]interface{}{
		"Posts":       result.Posts,
		"CurrentPage": result.CurrentPage,
		"TotalPages":  result.TotalPages,
		"HasPrev":     result.HasPrev,
		"HasNext":     result.HasNext,
	})
}
```

From internal/handler/blog/handler.go (template parsing):
```go
pages := []string{"list.html", "post.html", "404.html"}
// Each page parsed as: template.Must(template.New("").Funcs(funcMap).ParseFS(web.Templates, "templates/base.html", "templates/"+page))
// This means {{define "meta"}} in post.html overrides {{block "meta"}} in base.html within the same template set.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Excerpt to PostDetail and pass it from ShowPost handler</name>
  <files>
    internal/service/post/get.go,
    internal/handler/blog/post.go
  </files>
  <read_first>
    internal/service/post/get.go,
    internal/service/post/excerpt.go,
    internal/handler/blog/post.go,
    internal/handler/blog/handler_test.go
  </read_first>
  <action>
**1. Add Excerpt field to PostDetail** in `internal/service/post/get.go`:

Add a new field to the `PostDetail` struct:
```go
type PostDetail struct {
	Post         model.Post
	RenderedHTML template.HTML
	ToC          []ToCEntry
	Tags         []string
	Excerpt      string    // plain-text excerpt for OG description
	ReadingTime  int
}
```

In the `GetBySlug` method, compute and set the Excerpt field using the existing `Excerpt()` function:
```go
return &PostDetail{
	Post:         *p,
	RenderedHTML: template.HTML(enrichedHTML),
	ToC:          ExtractToC(enrichedHTML),
	Tags:         ParseTags(p.Tags),
	Excerpt:      Excerpt(p.Body, 200),
	ReadingTime:  ReadingTime(p.Body),
}, nil
```

**2. Pass Excerpt in ShowPost handler** in `internal/handler/blog/post.go`:

Add `"Excerpt"` to the template data map in the `h.render` call:
```go
h.render(w, http.StatusOK, "post.html", map[string]interface{}{
	"Post":         detail.Post,
	"RenderedHTML": detail.RenderedHTML,
	"ToC":          detail.ToC,
	"Tags":         detail.Tags,
	"ReadingTime":  detail.ReadingTime,
	"Excerpt":      detail.Excerpt,
})
```
  </action>
  <verify>
    <automated>cd /Users/jaredwallace/src/jared-wallace/website-go && go build ./... && go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - internal/service/post/get.go contains `Excerpt      string` in the PostDetail struct
    - internal/service/post/get.go contains `Excerpt:      Excerpt(p.Body, 200)`
    - internal/handler/blog/post.go contains `"Excerpt":      detail.Excerpt`
    - `go build ./...` exits 0
    - `go test ./...` exits 0
  </acceptance_criteria>
  <done>PostDetail has Excerpt field computed from markdown body. ShowPost handler passes Excerpt to post.html template. All existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add OG meta block, RSS auto-discovery, fallback image, and OG meta tests</name>
  <files>
    web/templates/base.html,
    web/templates/post.html,
    web/templates/list.html,
    web/static/og-fallback.png,
    internal/handler/blog/handler_test.go
  </files>
  <read_first>
    web/templates/base.html,
    web/templates/post.html,
    web/templates/list.html,
    internal/handler/blog/handler_test.go,
    .planning/phases/04-distribution/04-UI-SPEC.md,
    .planning/phases/04-distribution/04-RESEARCH.md
  </read_first>
  <action>
**1. Create OG fallback image** at `web/static/og-fallback.png`:

Generate a 400x400px PNG placeholder image. Use Go code, ImageMagick `convert`, or any available tool. The image should be:
- Background: #2C5F7A (the accent color from the design system)
- A simple solid-color square is acceptable as a placeholder per the UI-SPEC
- Dimensions: exactly 400x400 pixels (Twitter `summary` card minimum is 144x144, recommended is square)

If no image generation tool is available, create a minimal valid PNG programmatically using Go's `image/png` package as a one-off script, or use `convert -size 400x400 xc:'#2C5F7A' web/static/og-fallback.png` if ImageMagick is installed.

**2. Modify `web/templates/base.html`** — add two blocks inside `<head>`, before the closing `</head>` tag:

Add the RSS auto-discovery link (static, always present, per D-04):
```html
<link rel="alternate" type="application/rss+xml" title="The Log" href="/rss">
```

Add the OG meta extensibility block with site-level defaults (per D-08):
```html
{{block "meta" .}}
<meta property="og:title" content="The Log">
<meta property="og:description" content="dispatches from the deep end">
<meta property="og:type" content="website">
<meta property="og:image" content="https://jared-wallace.com/static/og-fallback.png">
<meta property="og:url" content="https://jared-wallace.com">
<meta name="twitter:card" content="summary">
<meta name="twitter:title" content="The Log">
<meta name="twitter:description" content="dispatches from the deep end">
<meta name="twitter:image" content="https://jared-wallace.com/static/og-fallback.png">
{{end}}
```

**3. Modify `web/templates/post.html`** — add OG meta override block. Place this AFTER the existing `{{define "title"}}...{{end}}` block and BEFORE `{{define "content"}}`:

```html
{{define "meta"}}
<meta property="og:title" content="{{.Post.Title}}">
<meta property="og:description" content="{{.Excerpt}}">
<meta property="og:type" content="article">
<meta property="og:image" content="https://jared-wallace.com/static/og-fallback.png">
<meta property="og:url" content="https://jared-wallace.com/posts/{{.Post.Slug}}">
<meta name="twitter:card" content="summary">
<meta name="twitter:title" content="{{.Post.Title}}">
<meta name="twitter:description" content="{{.Excerpt}}">
<meta name="twitter:image" content="https://jared-wallace.com/static/og-fallback.png">
{{end}}
```

**4. Modify `web/templates/list.html`** — add OG meta override block. Place this AFTER the existing `{{define "title"}}...{{end}}` block and BEFORE `{{define "content"}}`:

```html
{{define "meta"}}
<meta property="og:title" content="The Log">
<meta property="og:description" content="dispatches from the deep end">
<meta property="og:type" content="website">
<meta property="og:image" content="https://jared-wallace.com/static/og-fallback.png">
<meta property="og:url" content="https://jared-wallace.com">
<meta name="twitter:card" content="summary">
<meta name="twitter:title" content="The Log">
<meta name="twitter:description" content="dispatches from the deep end">
<meta name="twitter:image" content="https://jared-wallace.com/static/og-fallback.png">
{{end}}
```

NOTE: The list.html override has the same content as the base.html default. This is intentional — it makes the list page's OG tags explicit and independent, so future changes to base.html defaults don't accidentally affect the list page.

**5. Add OG meta tests** to `internal/handler/blog/handler_test.go`:

Add two new test functions using the existing `newTestHandler` and `mockRepository` pattern:

**TestPostOGMeta:** Create a mock with one published post (Title: "Test Post Title", Slug: "test-post", Body: "Some test body content for excerpt generation"). Make a GET request to `/posts/test-post` using `httptest.NewRecorder` and `http.NewRequest`. Set `r.SetPathValue("slug", "test-post")`. Call `handler.ShowPost(w, r)`. Assert the response body contains:
- `og:title" content="Test Post Title"` (post title in og:title)
- `og:type" content="article"` (article type for posts)
- `og:url" content="https://jared-wallace.com/posts/test-post"` (canonical URL with slug)
- `twitter:card" content="summary"` (Twitter card type)
- `og:image" content="https://jared-wallace.com/static/og-fallback.png"` (fallback image)

**TestListOGMeta:** Create a mock with one published post. Make a GET request to `/` using `httptest.NewRecorder` and `http.NewRequest`. Call `handler.ListPosts(w, r)`. Assert the response body contains:
- `og:title" content="The Log"` (site title in og:title)
- `og:type" content="website"` (website type for list)
- `og:description" content="dispatches from the deep end"` (site tagline)
- `twitter:card" content="summary"` (Twitter card type)
  </action>
  <verify>
    <automated>cd /Users/jaredwallace/src/jared-wallace/website-go && go build ./... && go test ./internal/handler/blog/... -run "TestPostOGMeta|TestListOGMeta" -v && go test ./... && test -f web/static/og-fallback.png && echo "OG image exists"</automated>
  </verify>
  <acceptance_criteria>
    - web/templates/base.html contains `<link rel="alternate" type="application/rss+xml" title="The Log" href="/rss">`
    - web/templates/base.html contains `{{block "meta" .}}`
    - web/templates/base.html contains `og:title" content="The Log"`
    - web/templates/base.html contains `twitter:card" content="summary"`
    - web/templates/base.html contains `og-fallback.png`
    - web/templates/post.html contains `{{define "meta"}}`
    - web/templates/post.html contains `{{.Post.Title}}`
    - web/templates/post.html contains `{{.Excerpt}}`
    - web/templates/post.html contains `og:type" content="article"`
    - web/templates/list.html contains `{{define "meta"}}`
    - web/templates/list.html contains `og:title" content="The Log"`
    - web/static/og-fallback.png exists and is a valid PNG file
    - internal/handler/blog/handler_test.go contains `func TestPostOGMeta(`
    - internal/handler/blog/handler_test.go contains `func TestListOGMeta(`
    - internal/handler/blog/handler_test.go contains `og:title`
    - `go test ./internal/handler/blog/... -run "TestPostOGMeta|TestListOGMeta"` exits 0
    - `go build ./...` exits 0
    - `go test ./...` exits 0
  </acceptance_criteria>
  <done>All public pages have OG and Twitter Card meta tags. Post pages show per-post OG title/description/URL. List and homepage show site-level OG tags. RSS auto-discovery link present on every page. Fallback OG image exists at /static/og-fallback.png. TestPostOGMeta and TestListOGMeta verify OG tag presence in handler responses. Templates parse and render without error.</done>
</task>

</tasks>

<verification>
1. `go build ./...` — templates still parse (ParseFS would panic at startup if block/define is malformed)
2. `go test ./internal/handler/blog/... -run "TestPostOGMeta|TestListOGMeta" -v` — OG meta tests pass
3. `go test ./...` — full suite green, no regressions
4. `test -f web/static/og-fallback.png` — OG image exists
</verification>

<success_criteria>
- Every public page includes og:title, og:description, og:type, og:image, og:url, twitter:card, twitter:title, twitter:description, twitter:image meta tags
- Post pages use dynamic values from PostDetail (title, excerpt, slug)
- Homepage/list pages use site-level values ("The Log", "dispatches from the deep end")
- RSS auto-discovery link present in head on every page
- OG fallback image file exists at web/static/og-fallback.png (400x400px)
- TestPostOGMeta asserts og:title contains post title in ShowPost response
- TestListOGMeta asserts og:title contains "The Log" in ListPosts response
- All tests pass, binary compiles
</success_criteria>

<output>
After completion, create `.planning/phases/04-distribution/04-02-SUMMARY.md`
</output>
