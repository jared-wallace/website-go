---
phase: 07-rebrand-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/templates/base.html
  - web/templates/list.html
  - web/templates/post.html
  - web/templates/404.html
  - web/templates/admin-base.html
  - web/templates/admin-login.html
  - internal/handler/blog/rss.go
  - internal/handler/blog/rss_test.go
  - internal/handler/blog/handler_test.go
  - web/static/main.css
autonomous: true
requirements:
  - BRAND-01
  - BRAND-02
  - BRAND-03
  - BRAND-04
  - NAV-01
  - NAV-02

must_haves:
  truths:
    - "Every public page title and nav header reads 'The Wild Meridian' instead of 'The Log'"
    - "RSS feed XML channel title reads 'The Wild Meridian'"
    - "OG and Twitter meta tags reference 'The Wild Meridian'"
    - "Copyright footer reads the current year followed by 'Jared Wallace' (not 'The Log')"
    - "Admin panel title and nav read 'The Wild Meridian -- Back Office'"
    - "A discreet RSS icon is visible in the footer, linking to /rss"
    - "HTML head contains link rel=alternate with title 'The Wild Meridian'"
  artifacts:
    - path: "web/templates/base.html"
      provides: "Rebranded public base template with RSS icon in footer"
      contains: "The Wild Meridian"
    - path: "web/templates/list.html"
      provides: "Rebranded list page title and meta"
      contains: "The Wild Meridian"
    - path: "web/templates/admin-base.html"
      provides: "Rebranded admin base template"
      contains: "The Wild Meridian -- Back Office"
    - path: "internal/handler/blog/rss.go"
      provides: "Rebranded RSS channel title"
      contains: "The Wild Meridian"
    - path: "web/static/main.css"
      provides: "RSS icon link styling"
      contains: ".rss-link"
  key_links:
    - from: "web/templates/base.html"
      to: "/rss"
      via: "RSS icon anchor in footer"
      pattern: "rss-link.*href=./rss."
    - from: "web/templates/base.html"
      to: "web/static/main.css"
      via: ".rss-link CSS class"
      pattern: "\\.rss-link"
    - from: "internal/handler/blog/rss_test.go"
      to: "internal/handler/blog/rss.go"
      via: "RSS title assertion"
      pattern: "The Wild Meridian"
---

<objective>
Rebrand the entire site from "The Log" to "The Wild Meridian" across all templates, RSS handler, and meta tags. Update copyright to "Jared Wallace". Add an RSS broadcast icon to the footer for feed discoverability. Update test assertions to match.

Purpose: Complete the v1.1 "Wild Meridian" identity transition and make RSS discoverable to readers.
Output: All 10 modified files reflecting the new brand, passing test suite, visible RSS icon in footer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-rebrand-navigation/07-CONTEXT.md
@.planning/phases/07-rebrand-navigation/07-RESEARCH.md
@.planning/phases/07-rebrand-navigation/07-UI-SPEC.md

<interfaces>
<!-- No new types or interfaces needed. This is a string replacement + HTML/CSS addition phase. -->
<!-- Key patterns to follow: -->

From web/templates/base.html (dark-toggle SVG pattern, lines 29-44):
The dark-toggle uses inline SVG with stroke="currentColor" inside a button.
The RSS icon must follow this identical pattern but inside an <a> tag.

From web/static/main.css (dark-toggle CSS pattern):
.dark-toggle uses color: var(--color-text-muted), hover: var(--color-accent),
min-width/min-height: 44px, display: flex, align-items/justify-content: center.
The .rss-link CSS must replicate this pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebrand all "The Log" strings to "The Wild Meridian" across templates, RSS handler, and tests</name>
  <files>
    web/templates/base.html,
    web/templates/list.html,
    web/templates/post.html,
    web/templates/404.html,
    web/templates/admin-base.html,
    web/templates/admin-login.html,
    internal/handler/blog/rss.go,
    internal/handler/blog/rss_test.go,
    internal/handler/blog/handler_test.go
  </files>
  <read_first>
    web/templates/base.html,
    web/templates/list.html,
    web/templates/post.html,
    web/templates/404.html,
    web/templates/admin-base.html,
    web/templates/admin-login.html,
    internal/handler/blog/rss.go,
    internal/handler/blog/rss_test.go,
    internal/handler/blog/handler_test.go
  </read_first>
  <action>
    Perform the following exact string replacements across 9 files. Each replacement is listed with file, line, old value, and new value.

    **web/templates/base.html** (6 replacements):
    - Line 6: `{{block "title" .}}The Log{{end}}` -> `{{block "title" .}}The Wild Meridian{{end}}`
    - Line 12: `title="The Log"` -> `title="The Wild Meridian"` (RSS alternate link, per D-01/NAV-02)
    - Line 14: `content="The Log"` -> `content="The Wild Meridian"` (og:title, per BRAND-03)
    - Line 20: `content="The Log"` -> `content="The Wild Meridian"` (twitter:title, per BRAND-03)
    - Line 27: `>The Log</a>` -> `>The Wild Meridian</a>` (nav .site-name, per BRAND-01)
    - Line 51: `&copy; {{.Year}} The Log` -> `&copy; {{.Year}} Jared Wallace` (copyright, per D-04/BRAND-04)

    **web/templates/list.html** (3 replacements):
    - Line 3: `{{define "title"}}The Log{{end}}` -> `{{define "title"}}The Wild Meridian{{end}}` (per BRAND-01)
    - Line 6: `content="The Log"` -> `content="The Wild Meridian"` (og:title, per BRAND-03)
    - Line 12: `content="The Log"` -> `content="The Wild Meridian"` (twitter:title, per BRAND-03)

    **web/templates/post.html** (1 replacement):
    - Line 3: `&#8212; The Log` -> `&#8212; The Wild Meridian` (title suffix, per BRAND-01)

    **web/templates/404.html** (1 replacement):
    - Line 3: `&#8212; The Log` -> `&#8212; The Wild Meridian` (title suffix, per BRAND-01)

    **web/templates/admin-base.html** (2 replacements, per D-03):
    - Line 6: `Admin{{end}} -- The Log` -> `Admin{{end}} -- The Wild Meridian` (browser tab title)
    - Line 16: `The Log -- Back Office` -> `The Wild Meridian -- Back Office` (nav .site-name)

    **web/templates/admin-login.html** (1 replacement, per D-03):
    - Line 5: `>The Log</h1>` -> `>The Wild Meridian</h1>` (login heading)

    **internal/handler/blog/rss.go** (1 replacement, per BRAND-02):
    - Line 91: `"The Log"` -> `"The Wild Meridian"` (Channel.Title field)

    **internal/handler/blog/rss_test.go** (1 replacement):
    - Line 45: `<title>The Log</title>` -> `<title>The Wild Meridian</title>` (test assertion)

    **internal/handler/blog/handler_test.go** (1 replacement):
    - Line 175: `og:title" content="The Log"` -> `og:title" content="The Wild Meridian"` (test assertion)

    IMPORTANT: Do NOT change any of these strings:
    - "dispatches from the deep end" (tagline in base.html, list.html OG description, rss.go Channel.Description)
    - "Back Office" standalone text in admin-login.html line 6 subtitle
    - rssManagingEditor constant in rss.go (already says "Jared Wallace")
  </action>
  <verify>
    <automated>cd /Users/jaredwallace/src/jared-wallace/website-go && go test ./internal/handler/blog/... -count=1 -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "The Log" web/templates/ internal/handler/blog/rss.go internal/handler/blog/rss_test.go internal/handler/blog/handler_test.go` returns zero matches
    - `grep "The Wild Meridian" web/templates/base.html` returns at least 5 matches (title, RSS alt, og:title, twitter:title, nav)
    - `grep "Jared Wallace" web/templates/base.html` returns at least 1 match (copyright footer)
    - `grep "The Wild Meridian" web/templates/list.html` returns 3 matches
    - `grep "The Wild Meridian -- Back Office" web/templates/admin-base.html` returns 1 match
    - `grep '"The Wild Meridian"' internal/handler/blog/rss.go` returns 1 match
    - `grep "The Wild Meridian" internal/handler/blog/rss_test.go` returns 1 match
    - `grep "The Wild Meridian" internal/handler/blog/handler_test.go` returns 1 match
    - `go test ./internal/handler/blog/...` exits 0
    - `grep "dispatches from the deep end" web/templates/base.html` still returns matches (tagline unchanged)
  </acceptance_criteria>
  <done>All 17 "The Log" occurrences replaced with "The Wild Meridian" (or "Jared Wallace" for copyright). Test suite passes. Tagline and other non-target strings unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add RSS broadcast icon to footer and CSS styling</name>
  <files>
    web/templates/base.html,
    web/static/main.css
  </files>
  <read_first>
    web/templates/base.html,
    web/static/main.css
  </read_first>
  <action>
    **web/templates/base.html** — Replace the footer `<p>` tag (which after Task 1 reads `&copy; {{.Year}} Jared Wallace`) with a version that includes the RSS icon anchor (per D-01, D-02, NAV-01):

    Replace:
    ```html
    <p>&copy; {{.Year}} Jared Wallace</p>
    ```

    With:
    ```html
    <p>
      &copy; {{.Year}} Jared Wallace
      <a href="/rss" class="rss-link" aria-label="RSS feed">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24"
             fill="none" stroke="currentColor" stroke-width="2"
             stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <path d="M4 11a9 9 0 0 1 9 9"></path>
          <path d="M4 4a16 16 0 0 1 16 16"></path>
          <circle cx="5" cy="19" r="1" fill="currentColor" stroke="none"></circle>
        </svg>
      </a>
    </p>
    ```

    The RSS icon is the Feather `rss` icon: circle at lower-left + two radiating arcs. The SVG uses `stroke="currentColor"` so it inherits color from the `.rss-link` anchor, matching the dark-toggle pattern.

    **web/static/main.css** — Add the `.rss-link` CSS rule after the existing `.site-footer p` rule (under the `/* --- Footer --- */` section, after approximately line 540):

    ```css
    .rss-link {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-width: 44px;
      min-height: 44px;
      margin-left: 8px;
      color: var(--color-text-muted);
      text-decoration: none;
      transition: color 200ms ease;
    }

    .rss-link:hover {
      color: var(--color-accent);
    }
    ```

    This mirrors the `.dark-toggle` pattern: 44px touch target, muted color at rest, accent on hover, 200ms transition.
  </action>
  <verify>
    <automated>cd /Users/jaredwallace/src/jared-wallace/website-go && grep -c "rss-link" web/templates/base.html && grep -c "rss-link" web/static/main.css && grep -c "aria-label=\"RSS feed\"" web/templates/base.html && go test ./... -count=1 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "rss-link" web/templates/base.html` returns at least 1 match
    - `grep 'href="/rss"' web/templates/base.html` returns at least 2 matches (one in head link, one in footer icon)
    - `grep "aria-label=\"RSS feed\"" web/templates/base.html` returns 1 match
    - `grep "aria-hidden=\"true\"" web/templates/base.html` returns at least 2 matches (dark-toggle SVG + RSS SVG)
    - `grep "min-width: 44px" web/static/main.css` returns at least 2 matches (dark-toggle + rss-link)
    - `grep "\.rss-link" web/static/main.css` returns at least 2 matches (base rule + hover rule)
    - `grep "color: var(--color-text-muted)" web/static/main.css` returns at least 2 matches (dark-toggle + rss-link)
    - `grep "color: var(--color-accent)" web/static/main.css` returns at least 2 matches (dark-toggle hover + rss-link hover)
    - `grep "currentColor" web/templates/base.html` returns matches for both dark-toggle and RSS SVGs
    - `go test ./...` exits 0 (full suite still green)
  </acceptance_criteria>
  <done>RSS broadcast icon visible in footer next to copyright, linking to /rss. Icon uses muted color at rest, accent on hover, with 44px touch target. Full test suite passes.</done>
</task>

</tasks>

<verification>
Phase-level verification after both tasks complete:

1. **Zero remaining "The Log" strings:**
   `grep -r "The Log" web/templates/ internal/handler/blog/` must return zero matches

2. **Full test suite green:**
   `go test ./...` must exit 0

3. **RSS icon present:**
   `grep "rss-link" web/templates/base.html web/static/main.css` must return matches in both files

4. **Tagline preserved:**
   `grep "dispatches from the deep end" web/templates/base.html` must still return matches

5. **Dynamic year preserved:**
   `grep "{{.Year}}" web/templates/base.html` must still return a match in the copyright footer
</verification>

<success_criteria>
- Every public page title, nav header, and meta tag reads "The Wild Meridian"
- RSS feed channel title is "The Wild Meridian"
- Copyright footer reads "(c) {year} Jared Wallace"
- Admin panel reads "The Wild Meridian -- Back Office"
- RSS broadcast icon in footer links to /rss with proper accessibility attributes
- All existing tests pass with updated assertions
- No regression: tagline, managing editor, and non-target strings unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/07-rebrand-navigation/07-01-SUMMARY.md`
</output>
