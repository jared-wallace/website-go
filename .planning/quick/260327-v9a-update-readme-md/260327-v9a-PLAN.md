---
phase: quick
plan: 260327-v9a
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
must_haves:
  truths:
    - "README project structure matches actual directory layout"
    - "No references to directories or packages that do not exist or are empty stubs"
    - "CI/deployment context mentioned so new contributors know the pipeline exists"
  artifacts:
    - path: "README.md"
      provides: "Accurate project documentation"
  key_links: []
---

<objective>
Update README.md so it accurately reflects the current state of the codebase after Phase 01 (Foundation) completion.

Purpose: The existing README was written speculatively — several directories listed don't match reality, and some real artifacts (CI workflow, database init dir) are absent. A correct README prevents the classic "the map is not the territory" confusion.
Output: Updated README.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md
@Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md to reflect actual project state</name>
  <files>README.md</files>
  <action>
Update the Project Structure section to accurately reflect the codebase:

```
cmd/server/        — Application entrypoint and server wiring
internal/          — Core packages
  config/          — Environment and app configuration
  database/        — PostgreSQL connection and pool setup
  handler/         — HTTP handlers (scaffolded, not yet implemented)
  markdown/        — Goldmark rendering pipeline with bluemonday sanitization
  model/           — Domain types (Post, etc.)
  server/          — Server setup and middleware
db/migrations/     — Goose SQL migrations (embedded at build time via embed.go)
web/               — Frontend assets
  templates/       — Go html/template files
  static/          — CSS and static files
.github/workflows/ — CI pipeline (lint, test, build)
```

Additional changes:
- Remove `database/` from the structure listing (it exists at root but is an empty init scaffold — not meaningful to document)
- Add a brief "CI" or "Continuous Integration" note after the Development section: single GitHub Actions workflow in `.github/workflows/ci.yml` runs lint, test, and build on push/PR. Mention it uses Go 1.26 in CI even though local dev works on Go 1.23+.
- Keep all other sections (Tech Stack, Prerequisites, Getting Started, Development/Makefile targets, License) as-is — they are accurate.
- Preserve the existing tone and formatting conventions.
  </action>
  <verify>
    <automated>grep -q "github/workflows" README.md && grep -q "markdown" README.md && grep -q "model" README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>README.md project structure matches the actual directory layout; CI workflow is mentioned; no phantom directories listed</done>
</task>

</tasks>

<verification>
- `cat README.md` and visually confirm structure matches `find . -type d` output
- No references to nonexistent paths
</verification>

<success_criteria>
README.md accurately documents the project structure, mentions CI, and contains no stale or speculative directory references.
</success_criteria>

<output>
After completion, create `.planning/quick/260327-v9a-update-readme-md/260327-v9a-SUMMARY.md`
</output>
